<UserControl x:Class="GrandSeal.Editor.Views.FontView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ScrollViewer Margin="4" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="MediumPurple">
            <Border BorderThickness="1" BorderBrush="Black" Width="Auto" Height="Auto" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image Source="{Binding Path=PreviewImage}" Width="{Binding Path=PreviewImage.Width}" Height="{Binding Path=PreviewImage.Height}" Margin="4" Stretch="None"/>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
